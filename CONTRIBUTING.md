# 贡献 OptiPlot

开发环境：`python -m pip install -e ".[dev]"`；运行 `python -m pytest -q`。

报告问题请附最小可分享的数据、列含义、实际/预期图型与 Python/库版本。请用模拟数据替代不能公开的实验数据。

贡献案例需要：论文 ID、来源 URL/DOI、年份、准确图号、变量模式、你编写的绘制配方、证据级别。明确是否亲自查看原图。期刊分区须带体系、年份、学科与可核对来源，会议使用自身评价口径。不要把搜索命中当作图像审阅。

贡献渲染器需说明：支持的数据形状、缺失值与重复点处理、统计定义、是否发生聚合/重采样、哪些变换由用户显式选择，以及 PNG/SVG/PDF 是否一致。

代码和原创结构笔记使用项目 MIT 许可；不要加入未获授权的论文图像、全文或实验数据。CI 工作流已配置，公开仓库的运行结果以实际执行为准。
